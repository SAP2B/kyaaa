[package]
name = "kyaaa"
version = "0.1.0"
edition = "2024"
description = "A zero-cost, no_std Heterogeneous List (HList) macro with named fields, surgical mutability, and zero allocation."
keywords = ["hlist", "no_std", "zero-cost", "macro", "data-structures"]
categories = ["data-structures", "no-std", "embedded", "memory-management"]
license = "AGPL-3.0-only"
repository = "https://github.com/SAP2B/kyaaa"
readme = "README.md"

[dependencies]

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[profile.dev]
panic = "abort"

[profile.release]
opt-level = 3
panic = "abort"
lto = true
codegen-units = 1
strip = true


[[bench]]
name = "webserver"
path = "benches/webserver.rs"
harness = false
